# Core aliases — always loaded. Mark with `# @doc:` to expose in `shmod doc`.
alias ll='ls -lah'  # @doc: Long listing with human-readable sizes
alias reload='source ~/.profile'  # @doc: Reload the shell profile
